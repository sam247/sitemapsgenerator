{
  "name": "shopify-sitemap-generator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd src && npm install",
    "start": "cd src && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 